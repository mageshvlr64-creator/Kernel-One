[project]
name = "document-pipeline"
version = "0.1.0"
description = "Document Ingestion service (docs/features/10_document_ingestion) — Sovereign AI Workbench"
requires-python = ">=3.12"
dependencies = ["pymupdf>=1.24"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
