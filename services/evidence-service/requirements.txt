# evidence-service runtime dependencies: none — the package is standard-library
# only (stdlib HTTP server, in-process storage). This file exists so CI and
# operators get an explicit, verified answer instead of guessing.
