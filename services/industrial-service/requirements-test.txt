-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
httpx>=0.27.0
# pgserver (embedded Postgres for tests/test_integration.py) is intentionally NOT
# pinned here: it ships no Linux wheels, so CI (ubuntu-latest) cannot install it.
# Those tests skip automatically when pgserver is absent (pytest.importorskip).
# Local (Windows/macOS): pip install pgserver  ->  integration tests run live.
